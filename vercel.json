{
  "functions": {
    "api/*": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/:slug", "destination": "/api/incident?slug=:slug" }
  ]
}
